import AddGuestPresentation from '@presentation/AddGuestPresentation';
import React from 'react';

const AddGuestContainer = () => {
  return <AddGuestPresentation />;
};

export default AddGuestContainer;
