import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const AddGuestPresentation = () => {
  return (
    <View>
      <Text>AddGuestPresentation</Text>
    </View>
  );
};

export default AddGuestPresentation;

const styles = StyleSheet.create({});
